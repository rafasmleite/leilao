<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>

    <session-factory>

        <!-- Configurações de conexão -->
        <property name="connection.driver_class">com.mysql.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/mydb</property>
        <property name="connection.username">root</property>
        <property name="connection.password">suporte@8569</property>

        <!-- Pool de conexões JDBC -->
        <property name="connection.pool_size">1000</property>

        <!-- Dialeto SQL -->
        <property name="dialect">org.hibernate.dialect.MySQL5InnoDBDialect</property>

        <!-- Gerenciamento sessão -->
        <property name="current_session_context_class">thread</property>

        <!-- Desabilita cache  -->
        <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

        <!-- Show sql -->
        <property name="show_sql">true</property>

        <!-- Cria Tabela -->
        <property name="hbm2ddl.auto">update</property>
        <!-- <property name="hbm2ddl.auto">validate</property> -->

        <!-- <mapping resource="org/hibernate/tutorial/domain/Event.hbm.xml"/> -->
		<mapping class="br.com.leilao.domain.Estado" />
		<mapping class="br.com.leilao.domain.Cidade" />
		<mapping class="br.com.leilao.domain.Empresa" />
		<mapping class="br.com.leilao.domain.Cargo" />
		<mapping class="br.com.leilao.domain.Usuario" />
		<mapping class="br.com.leilao.domain.TipoCliente" />
		<mapping class="br.com.leilao.domain.TipoEntrega" />
    </session-factory>

</hibernate-configuration>